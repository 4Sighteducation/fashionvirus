# Fashion Inc. Phase 0 — Risk Register

Audit date: 2026-08-04. Severity: **Critical / High / Medium / Low**. Each risk cites its evidence label from the companion audit reports.

| # | Risk | Severity | Evidence | Mitigation | Status |
|---|---|---|---|---|---|
| R1 | **4.2 GB of Synty packs committed into the Fashion Virus repo by accident** (`git add -A` habit), bloating history irreversibly and coupling the two products | Critical | [Verified] packs sit untracked inside the repo working tree | `.gitignore` guard rail added during this audit; move packs out of the repo tree when the sibling folder is created | **Mitigated (guard rail)**, pending relocation |
| R2 | **Licence exposure: purchased Synty source assets pushed to a PUBLIC repository.** `4Sighteducation/fashioninc` is currently public | Critical | [Verified] `gh repo view` → visibility PUBLIC | Flip repo to private before first push; better, never commit raw packs at all (see SYNTY_ASSET_AUDIT §6) | **Open — action required before any push** |
| R3 | **No web-ready assets exist** (0 glTF/GLB). Underestimating the FBX→glTF pipeline could stall the first visual milestone | High | [Verified] format census | Stand up a scripted conversion pipeline as an early Phase 1 task; validate one building + one character end-to-end before bulk conversion | Open |
| R4 | **Missing content for the PoC's emotional core**: no garment/utility-jacket hero asset, no factory/dye-house interiors, no polluted-river variants | High | [Verified] pack inventory; [Inferred] gap analysis | Kit-bash early; commission/model the hero jacket; plan river states as materials/shaders not meshes | Open |
| R5 | **TypeScript `strict` is off** in the codebase whose patterns (and possibly code) Fashion Inc. will inherit. A deterministic simulation kernel without strict null-safety invites silent state corruption | High | [Verified] tsconfig.app.json | New repo starts with `strict: true` + `noUncheckedIndexedAccess`; any code ported from Fashion Virus is strict-fixed at the border | Open (decision for new repo) |
| R6 | **Non-deterministic RNG precedent**: `src/game/engine.ts` uses bare `Math.random`; Fashion Inc.'s laws demand one authoritative deterministic world state | High | [Verified] engine source | Simulation kernel uses a seeded, serialisable RNG from day one (`src/engine/island.ts` `makeRng` is the in-house precedent) | Open (design law) |
| R7 | **No tests and no CI anywhere.** Acceptable for a card game; fatal for a simulation whose selling point is lawful, reproducible behaviour | High | [Verified] repo audit §6 | New repo ships with vitest + a determinism regression test (same seed → same world hash) + GitHub Actions from the first commit | Open |
| R8 | **Single shared Supabase project temptation.** Mixing Fashion Inc. tables into the Fashion Virus project couples quotas, key rotation, and RLS mistakes across products | Medium | [Verified] one project exists; [Inferred] coupling risk | Create a second Supabase project in the same org; copy the proven write-only telemetry pattern | Open |
| R9 | **Supabase MCP/live-state drift**: live schema could not be re-verified today (MCP timeout); migrations and dashboard could silently diverge | Medium | [Unknown] live state as of audit | Treat `supabase/migrations/` as source of truth; re-verify with `supabase db diff` or MCP when available | Open |
| R10 | **Node version unpinned** (no `.nvmrc`/`engines`) — works today, but a Vercel default-runtime bump or contractor with a different Node could produce divergent builds | Medium | [Verified] repo audit §2 | Pin `engines.node` in the new repo (and optionally in Fashion Virus) | Open |
| R11 | **Entire.io not verifiably configured** — no local CLI, config, or Cursor hooks found; "linked to all repos" could not be confirmed from the machine. Development-memory expectations may be unmet | Medium | [Verified] absence locally; [Unknown] GitHub-side linkage | Confirm Entire's integration point (GitHub App vs local hooks); configure redaction/exclusions before it ever sees `.env.local` or Synty paths | Open |
| R12 | **fashioninc repo is empty with no branch protection**; first pushes will define conventions (LFS or not, folder layout) that are expensive to reverse | Medium | [Verified] `isEmpty: true` | First commit should be scaffolding + `.gitignore` + `ASSET_PROVENANCE.md` + CI, agreed via the repository recommendation before any code | Open |
| R13 | **Blueprint scale vs. build reality**: the manifesto is 58,626 lines / 25 chapters; scope creep into the PoC ("one world, one jacket, one rival, one river, one crisis, one legacy snapshot") is the single biggest schedule risk | Medium | [Verified] document size; PoC law from kickoff | Enforce the stage-gate roadmap; every feature request checked against the PoC constitution | Open (process) |
| R14 | **Design docs live only in an untracked folder** (`Fashion_Inc/Docs`, now gitignored) — a disk failure loses the canonical blueprint versions | Medium | [Verified] untracked + ignored | Commit Docs to the new private repo as its first content (they are text, a few MB) | Open — quick win |
| R15 | **Fashion Virus regression risk during the transition** — shared machine, shared Supabase, shared habits | Low | [Inferred] | Keep repos, Vercel projects, and Supabase projects fully separate; Fashion Virus stays frozen except for its own roadmap | Managed by separation |

## Watch-list (not yet risks)

- Zustand is installed but unused in Fashion Virus [Verified] — remove or use; harmless but signals drift.
- The Synty packs' nested `.gitattributes`/`.gitignore` files will interfere if the packs are ever committed as-is [Verified] — another reason not to.
- `golden-rules-one-page-v0.1.39.md` referenced by the kickoff prompt is not in `Fashion_Inc/Docs` [Verified] — the rules exist inside the manifesto (Chapter 25); regenerate the one-pager or drop the reference.
